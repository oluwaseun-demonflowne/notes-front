import SideBar from "@/components/Profile/SideBar";
import React from "react";

const ListFolder = () => {
  return (
    <div className="chat h-[100%] overflow-y-scroll">
      <SideBar />
    </div>
  );
};

export default ListFolder;
